= render :partial => "layouts/dashboard_setting_tab"

= simple_form_for [:dashboard, @style], class: 'form form-horizontal', multipart: true  do |f|
  = f.input :name, label: "风格名称"
  = f.submit f.object.persisted? ? "修改" : "增加", class: 'btn btn-primary btn-default'

